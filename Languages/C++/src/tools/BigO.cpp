//
// Created by dubjr on 3/27/2017.
//

#include "../Headers/BigO.h"
#define <string>

namespace ComplexityAnalysis{

    template <class T>
    void BigO::addOneToCounter(T *curr) {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }

    template <class T>
    void BigO::attach(T *item) {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }

    template <class T>
    void BigO::detach(T *item) {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }

    template <class T>
    int BigO::getComplexity() {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }

    template <class T>
    int BigO::getFollowingId(T *item) {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }

    template <class T>
    void BigO::setupN(T *curr, int N) {
        if(!is_type_in_namespace<T>("Algo::")){

            printf("Sorry, your passed item is not a part of the %s namespace", );
        }
    }
};